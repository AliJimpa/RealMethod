using UnityEngine;
using RealMethod;

[CreateAssetMenu(fileName = "#SCRIPTNAME#", menuName = "#PROJECTNAME#/#SCRIPTNAME#", order = 1)]
public class #SCRIPTNAME# : BuffConfig<STATTYPE> // find your STATTYPE enum in StatProfile
{
#if UNITY_EDITOR
    private void OnValidate()
    {
        ChangeName(name);
    }
#endif
}